name: Govulncheck auto-fix

on:
  schedule:
    - cron: '0 6 * * *'  # daily 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  autofix:
    uses: sloccy/go-vuln-autofix/.github/workflows/vuln-autofix.yml@main
    secrets: inherit
